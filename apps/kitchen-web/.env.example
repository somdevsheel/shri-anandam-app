# Read by both the browser and the Route Handlers under app/api/auth/*
# (server-side) — same convention as apps/admin-web (ADR-020).
# Production: https://api.shrianandamsweets.in/api/v1 — this app itself
# deploys to https://kitchen.shrianandamsweets.in (see
# docs/deployment/domain-and-dns.md).
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
