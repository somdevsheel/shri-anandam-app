# Local development only — EAS build profiles (eas.json) set this per
# environment for staging/production builds. Expo inlines any
# EXPO_PUBLIC_* variable into the JS bundle at build time, so never put
# a secret here.
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1
